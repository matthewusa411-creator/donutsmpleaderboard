name: Build Mod

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 25
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '25'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '9.4'

      - name: Build mod
        run: gradle build

      - name: Upload built jar
        uses: actions/upload-artifact@v4
        with:
          name: leaderboard-hud-jar
          path: build/libs/*.jar
